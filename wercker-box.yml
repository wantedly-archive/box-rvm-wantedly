name : rvm-wantedly
version: 0.0.1
inherits: wercker/rvm
type: main
platform: ubuntu@12.04
description: A wercker rvm box with extension for Wantedly rails build
keywords:
  - ruby
  - libicu
packages:
  - libicu-dev
script: |
  sudo apt-get install -y libicu-dev
