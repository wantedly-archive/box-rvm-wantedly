name: rvm-wantedly
version: 0.0.3
inherits: wercker/rvm@2.5.0
type: main
platform: ubuntu@12.04
description: wercker box for rvm with some extension
keywords:
  - ruby
  - libicu
script: |
  sudo apt-get update
  sudo apt-get install -y libicu-dev cmake
